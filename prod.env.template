# c8ctl "prod" profile credentials — template.
#
# Copy this file to prod.env and fill in the values (prod.env is git-ignored).
# Then add the profile without exposing secrets on the command line:
#
#   c8ctl add profile prod --from-file=prod.env
#
# Rotate the client secret in the Camunda Console if it was ever pasted
# into a terminal, chat, or transcript.

CAMUNDA_BASE_URL=https://sin-1.api.camunda.io/13ed88e1-3ffb-4d96-b6b0-3248261f352a
CAMUNDA_CLIENT_ID=
CAMUNDA_CLIENT_SECRET=
